@model IEnumerable<QuanLyGiangDay.Models.TMonHoc>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Môn Học</h1>

<p>
    <a asp-action="Create">Thêm</a>
</p>
<form asp-action="Index" method="get">
    <p>
        Tìm kiếm: <input type="text" name="searchString" value="@ViewData["CurrentFilter"]" />
        <input type="submit" value="Tìm kiếm" />
    </p>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenMonHoc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoTietLyThuyet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoTietThucHanh)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenMonHoc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoTietLyThuyet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoTietThucHanh)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaMonHoc">Sửa</a> |
                <a asp-action="Details" asp-route-id="@item.MaMonHoc">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.MaMonHoc">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
